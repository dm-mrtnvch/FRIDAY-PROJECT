

const initialState = {

}

const recoverPasswordReducer = (state = initialState, action: any) => {
    switch (action.type){
        default:
            return state
    }
}

export default recoverPasswordReducer