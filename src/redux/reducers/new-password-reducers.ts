

const initialState = {

}

const newPasswordReducer = (state = initialState, action: any) => {
    switch (action.type){
        default:
            return state
    }
}

export default newPasswordReducer